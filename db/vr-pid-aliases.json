{
  "_comment": "Retired politician_id → canonical politician_id. `politician_id` is free text across the vr_* tables (there is no politicians table and no FK), so the same person can end up under two ids — a curated seed using one and the live ingest another. When a merge migration folds one id into another, record the retirement here: the ingest canonicalizes on write and the Voting Record API canonicalizes on read, so a stale Blobs member-map override, an old bookmark, or a saved client record can never re-open the split. Client-side stance lookups have their own bridge (STANCE_ALIASES in stance-helpers.js) — keep the two in step.",
  "aliases": {
    "susan_collins": "collins",
    "kennedy_rfk": "rfkjr",
    "cullimore_s19": "kcullimore",
    "calbrecht": "carl_albrecht",
    "derek_brown": "derek_brown_ut"
  },
  "notes": {
    "susan_collins": "Susan Collins (R-ME, C001035). Merged by netlify/database/migrations/20260725050000_vr_merge_susan_collins_identity.sql. `collins` is canonical: it is the cmp-data.js roster id, carries the richer sourced stance block, and held 19 of her 22 roll-call votes.",
    "kennedy_rfk": "Robert F. Kennedy Jr. (HHS Secretary). No merge migration: he holds NO DB rows under either id — 0 in vr_member_votes, 0 in vr_positions, 0 pdx_proposals links (verified against the branch DB, July 25 2026) — because a cabinet officer casts no roll-call votes. This was a curated-content split, not a vote split, so it was fixed in the data files: the six sourced stance cards moved into the `rfkjr` block in politician-stances.js, the duplicate cmp-data.js roster record was removed, and the one spotlight card was re-keyed. `rfkjr` is canonical: it carries every deep-dive surface (cmp-data-detail.js, spotlight-cards-data.js, acct-spotlight-data.js, the index.html votingRecords block and profile blurb), the repaired official portrait, termStart tenure, and 3 of the 4 spotlight cards. The alias still matters even with no rows: the ingest canonicalizes on write and the Voting Record API on read, so if a future source ever attributes a row to `kennedy_rfk` it lands on `rfkjr` instead of re-opening the split.",
    "cullimore_s19": "Kirk A. Cullimore Jr. (R, Utah State Senate; Senate Majority Leader since Jan 21 2025). ONE person under two ids, and the districts in the two records were not a contradiction but a timeline: he was elected in 2018 to Utah Senate District 9, and the post-2020-census redistricting renumbered his Salt Lake County seat to District 19 effective Jan 1 2023 (Wikipedia/Ballotpedia; his own campaign site still prints the old number). District 9 is now held by a DIFFERENT senator, Jen Plumb (D-Salt Lake City) — already in the roster as `jen_plumb_s9` — so the stale `kcullimore` label was actively attributing another member's district to him. No merge migration: he holds NO DB rows under either id — 0 in vr_member_votes, 0 in vr_positions, 0 pdx_proposals links (verified against the branch DB, July 25 2026), as the vr_* tables carry congressional roll calls and he is a state legislator. Fixed in the data files instead: the duplicate cmp-data.js roster record was removed after its district was corrected to 19, the `cullimore_s19` bio was folded into the `kcullimore` cmp-data-detail.js record, and the ballot/incumbent references were re-keyed. `kcullimore` is canonical: it carries the promise score (14 kept / 6 broken / 5 pending), the real detail stances, the official le.utah.gov portrait, the profile blurb, both spotlight layers (spotlight-cards-data.js and acct-spotlight-data.js), four deep-dive script surfaces, and index.html's ACCT_ALIAS already pointed `cullimore_s19` at it. Curated stance cards stay under the name-slug key `kirk_cullimore` per this file's stance-key convention; STANCE_ALIASES bridges both ids to it.",
    "calbrecht": "Carl R. Albrecht (R, Utah House District 70 — Richfield, Sevier County; in office since 2017). ONE person under two ids, split by SURFACE rather than by district: `carl_albrecht` held the identity layer (the cmp-data.js roster record, the index.html browse-directory profile with bio/quote/promises, _UTAH_HOUSE_INFO d:70, KR_STATE_HOUSE_INCUMBENTS[70], the spotlight-cards-data.js group) plus three UNSOURCED general stance cards, while `calbrecht` held the sourced content layer (six bill-sourced stance cards, the acct-spotlight-data.js Evidence Locker group, the ACCT_THEME profile blurb, a consistency.js headline mapping, and a Power-Map META row mis-tagged 'STATE SENATE' that could never render because pmInjectDynamicCards only injects ids present in PROFILES). No merge migration: as with `cullimore_s19` there are no DB rows to move under either id — the vr_* tables carry congressional roll calls and he is a state legislator, so no roll-call row can be attributed to him — and that is a statement about the schema, not a fresh branch-DB count taken in this pass, which is exactly what the alias covers: the ingest canonicalizes on write and the Voting Record API on read, so a future source attributing a row to `calbrecht` lands on `carl_albrecht` instead of re-opening the split. `carl_albrecht` is canonical because it is the ROSTER id — making the id with no cmp-data.js record canonical would have meant re-pointing the roster, the browse node and both Utah maps at a key that names nobody, and STANCE_ALIASES' own advice is to keep the stance-card key equal to the roster id so no alias is needed. Merged by scripts/merge-albrecht-dedupe-topics-jul2026.mjs (idempotent, dry-run by default): all six sourced cards moved into the `carl_albrecht` block ahead of its three unsourced ones (an issueKey-only findStance() lookup must resolve the sourced card first), and the other four surfaces were re-keyed. index.html's ACCT_ALIAS keeps `calbrecht` → `carl_albrecht` so a saved pick under the old id still resolves. The three inherited unsourced cards remain the record's one quality gap; see scripts/UTAH-LAUNCH-CLEANUP-TRACKER.md.",
    "derek_brown": "Derek Brown (R, Utah Attorney General since Jan 2025). ONE person under two ids, split by SURFACE and the only true duplicate among the 25 Utah 'surface-split' pairs swept in July 2026 — the other 24 are a single roster record whose curated stance block is keyed on a slug of the person's display name, which is this file's documented stance-key convention and not a duplicate identity at all. Here both ids carried real content: 'derek_brown_ut' held the cmp-data.js roster record (State Attorney General, Utah, score 53) plus four thin AG-wave stance cards, while 'derek_brown' held four richer bill-sourced cards (the TikTok and Snap child-safety litigation, the 'return trust to the office' pledge succeeding Sean Reyes, federalism and public lands, fentanyl enforcement) and three spotlight cards. Because _resolveStanceList() tries ISSUE_STANCE_DATA[id] before any fallback, the roster id's thinner block won and all four sourced cards were unreachable from the canonical profile. No merge migration: he holds no DB rows under either id — the vr_* tables carry congressional roll calls and a state attorney general casts none, so no roll-call row can be attributed to him — which is a statement about the schema rather than a fresh branch-DB count taken in this pass. That is exactly what the alias covers: the ingest canonicalizes on write and the Voting Record API on read, so a future source attributing a row to 'derek_brown' lands on 'derek_brown_ut' instead of re-opening the split. 'derek_brown_ut' is canonical because it is the ROSTER id, the same rule used for 'carl_albrecht'. Merged by scripts/merge-utah-surface-splits-jul2026.mjs (idempotent, dry-run by default): the four sourced cards were grafted ABOVE the four originals (two issueKeys collide — tech_balance and lands_local — and an issueKey-only findStance() lookup must resolve the sourced card first; no topic string collides, so section 7 stays green), the three spotlight cards were re-keyed, and index.html's ACCT_ALIAS keeps 'derek_brown' → 'derek_brown_ut' so a saved pick under the old id still resolves. The four inherited AG-wave cards are the record's remaining quality gap: all four cite only the office's own homepage. See scripts/UTAH-LAUNCH-CLEANUP-TRACKER.md."
  }
}
